import 'package:flutter/material.dart';

class DMAppColors {
  static const Color primary = Color(0xFFC8E5E3);      // Soft Teal
  static const Color primaryShade = Color(0xFFD4E7E7); // Lighter Teal Shade
  static const Color secondary = Color(0xFF3A6F76);    // Deep Teal
  // static const Color stockable = Color(0xFF52D361);    // Stockable color
  static const Color stockable = Color(0xFF97DA9E);    // Stockable color
  // static const Color nonStockable = Color(0xFFFECB5D); // Non Stockable color
  static const Color nonStockable = Color(0xFFEAC986); // Non Stockable color
  // static const Color nonMoving = Color(0xFFEAABB5);    // Non Moving color
  static const Color nonMoving = Color(0xFFE6C3C9);    // Non Moving color

  // Additional derived/app-friendly colors
  static const Color background = Color(0xFFF5FAFA);   // Very light background
  static const Color accent = Color(0xFF5CA8A2);       // Accent teal for buttons/highlights
  static const Color darkAccent = Color(0xFF2D5D62);   // Deeper tone for text/icons on accent
  static const Color border = Color(0xFFB6D1CF);       // Soft border color
  static const Color surface = Color(0xFFEAF4F3);      // Surface like cards
  static const Color textPrimary = Color(0xFF1C2B2D);  // Main text color
  static const Color textSecondary = Color(0xFF5A777B); // Subdued text
  static const Color warning = Color(0xFFF4C430);      // Yellowish warning
  static const Color error = Color(0xFFCF3A3A);        // Error red
  static const Color success = Color(0xFF48A999);      // Confirmation/success green
}



// accent: Use for interactive elements (buttons, highlights).
//
// darkAccent: For text/icons on accent.
//
// surface: For cards or elevated components.
//
// border: For outlines or dividers.
//
// textPrimary/textSecondary: For content hierarchy.
//
// success, warning, error: For status messages, indicators.
